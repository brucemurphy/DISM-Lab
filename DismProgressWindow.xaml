<Window x:Class="YourNamespace.DismProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DISM Progress"
        Height="200" Width="200"
        MinHeight="200" MinWidth="200"
        MaxHeight="200" MaxWidth="200"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="DISM Operation Progress" FontSize="16" FontWeight="Bold" />
        <!-- Start hidden until process begins -->
        <ProgressBar x:Name="ProgressBar"
                     Grid.Row="1"
                     Height="20"
                     Minimum="0"
                     Maximum="100"
                     Margin="0,8,0,8"
                     Visibility="Collapsed"
                     IsIndeterminate="False"/>
        <Border Grid.Row="2" BorderBrush="#444" BorderThickness="1" CornerRadius="4" Padding="4" Background="#111">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="OutputTextBox"
                         FontFamily="Consolas"
                         FontSize="12"
                         IsReadOnly="True"
                         TextWrapping="NoWrap"
                         Background="#111"
                         Foreground="#ddd"
                         BorderThickness="0"/>
            </ScrollViewer>
        </Border>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Width="100" Margin="0,0,10,0" />
            <Button Content="Close" Width="100" x:Name="CloseButton" />
        </StackPanel>
    </Grid>
</Window>