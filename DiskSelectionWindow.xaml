<Window x:Class="DiskSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select USB Disk"
        Height="360"
        Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0F0F0F"
        Foreground="#F5F5F5"
        ShowInTaskbar="False">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#151515" />
        <SolidColorBrush x:Key="AccentBrush" Color="#4C9EFB" />
        <SolidColorBrush x:Key="HeaderBrush" Color="#202020" />
        <SolidColorBrush x:Key="BorderBrushDark" Color="#2A2A2A" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="#BF000000" />
            <Setter Property="Foreground" Value="#FFFAFAFA" />
            <Setter Property="BorderBrush" Value="#FF3F3F3F" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3FFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#5FFFFFFF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FF2D2D2D" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF2D2D2D" />
                                <Setter Property="Foreground" Value="#FF9D9D9D" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Foreground" Value="#F0F0F0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewColumnHeader">
                        <Border Background="{StaticResource HeaderBrush}"
                                BorderBrush="#2F2F2F"
                                BorderThickness="0,0,0,1">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="2" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <GridViewRowPresenter Columns="{Binding RelativeSource={RelativeSource AncestorType=ListView}, Path=View.Columns}"
                                                  Content="{TemplateBinding Content}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#263447" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#1F2531" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">
            <TextBlock Text="Select the disk that should be cleaned and prepared for the WinPE USB."
                       FontSize="15"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4" />
            <TextBlock Text="Only removable USB media are shown."
                       Foreground="#9BA3AE"
                       FontSize="12" />
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,10,0,12"
                Padding="8"
                Background="{StaticResource PanelBrush}"
                CornerRadius="10"
                BorderBrush="{StaticResource BorderBrushDark}"
                BorderThickness="1">
            <Grid>
                <ListView x:Name="DiskListView"
                          BorderThickness="0"
                          Background="Transparent"
                          Foreground="White"
                          SelectionMode="Single"
                          MouseDoubleClick="DiskListView_MouseDoubleClick"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.View>
                        <GridView AllowsColumnReorder="False">
                            <GridViewColumn Header="Disk #" Width="60" DisplayMemberBinding="{Binding Number}" />
                            <GridViewColumn Header="Model" Width="210">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Model}" TextTrimming="CharacterEllipsis" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Size" Width="90" DisplayMemberBinding="{Binding DisplaySize}" />
                            <GridViewColumn Header="Connection" Width="90" DisplayMemberBinding="{Binding Connection}" />
                            <GridViewColumn Header="Media" Width="90" DisplayMemberBinding="{Binding MediaType}" />
                        </GridView>
                    </ListView.View>
                </ListView>

                <TextBlock x:Name="EmptyStateText"
                           Text="No USB disks were detected. Attach a removable drive and refresh."
                           Foreground="#9BA3AE"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Collapsed"
                           TextAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="32,0" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,4,0,0">
            <TextBlock Text="WARNING: The selected disk will be cleaned. Ensure the correct removable disk is selected."
                       Foreground="#EE0000"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button x:Name="RefreshDisksButton"
                        Content="Refresh"
                        Width="110"
                        Click="RefreshDisksButton_Click"
                        HorizontalAlignment="Left"
                        ToolTip="Rescan for removable USB disks" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Width="100" Margin="0,0,8,0" Click="CancelButton_Click" />
                    <Button Content="Use Disk" Width="120" Click="OkButton_Click" />
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Window>
